server:
  port: 8888
spring:
  profiles:
    active:
    - native 
  cloud:
    config:
      server:
        native:
          search-locations:
          - file:///c:\temp\config
        #git:
          #uri: https://github.com/salvadorsobrino/servicios_config
          